---
import { ExternalLink, MessageCircle } from "@lucide/astro";
import { contactConfig, whatsappQuoteMessage } from "../data/contact";

interface Props {
  class?: string;
  fallbackLabel?: string;
  label?: string;
  message?: string;
  tone?: "default" | "inverted";
  unavailableLabel?: string;
}

const {
  class: className = "",
  fallbackLabel = "Contactar por Instagram",
  label = "Cotizar por WhatsApp",
  message = whatsappQuoteMessage,
  tone = "default",
  unavailableLabel = "WhatsApp próximamente",
} = Astro.props;

const whatsappNumber = contactConfig.whatsappNumber.replace(/\D/g, "");
const whatsappHref = whatsappNumber
  ? `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`
  : "";
const instagramHref = contactConfig.instagramUrl;
const href = whatsappHref || instagramHref;
const buttonLabel = whatsappHref
  ? label
  : instagramHref
    ? fallbackLabel
    : unavailableLabel;
const baseClasses =
  "inline-flex min-h-11 w-full items-center justify-center rounded-md px-5 py-3 text-center text-sm font-semibold transition-colors sm:w-auto";
const toneClasses = {
  default: {
    active:
      "bg-neutral-950 text-white hover:bg-neutral-800 focus:outline-none focus-visible:ring-2 focus-visible:ring-neutral-950 focus-visible:ring-offset-2",
    inactive:
      "cursor-not-allowed border border-neutral-200 bg-neutral-100 text-neutral-500",
  },
  inverted: {
    active:
      "bg-white text-neutral-950 hover:bg-neutral-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-neutral-950",
    inactive:
      "cursor-not-allowed border border-white/15 bg-white/10 text-neutral-400",
  },
};

const activeClasses = toneClasses[tone].active;
const inactiveClasses = toneClasses[tone].inactive;
---

{
  href ? (
    <a
      class={`${baseClasses} ${activeClasses} ${className}`}
      href={href}
      rel="noopener noreferrer"
      target="_blank"
    >
      {whatsappHref ? (
        <MessageCircle class="mr-2 size-4" stroke-width={1.8} aria-hidden="true" />
      ) : (
        <ExternalLink class="mr-2 size-4" stroke-width={1.8} aria-hidden="true" />
      )}
      {buttonLabel}
    </a>
  ) : (
    <span
      aria-disabled="true"
      class={`${baseClasses} ${inactiveClasses} ${className}`}
      role="link"
    >
      {buttonLabel}
    </span>
  )
}
