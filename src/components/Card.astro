---
interface Props {
  as?: "article" | "div" | "li";
  class?: string;
  padding?: "default" | "none";
  variant?: "white" | "muted" | "paper" | "dark";
}

const {
  as: Tag = "article",
  class: className = "",
  padding = "default",
  variant = "white",
} = Astro.props;

const variantClasses = {
  white: "bg-white",
  muted: "bg-neutral-50",
  paper: "bg-yivalte-paper",
  dark: "border-white/10 bg-neutral-950 text-white",
};

const paddingClasses = {
  default: "p-5",
  none: "",
};
---

<Tag
  class={`rounded-lg border border-neutral-200 ${variantClasses[variant]} ${paddingClasses[padding]} shadow-sm shadow-neutral-950/5 ${className}`}
>
  <slot />
</Tag>
